<%# v3.0 %>

<%= render HawthorneCore::UI::Form::SelectFieldComponent.new(
  form: @form,
  attribute: :country_code_alpha2,
  label: 'Country',
  options: options_from_collection_for_select(@countries, :code_alpha2, :handle, @selected_country&.code_alpha2),
  prompt: 'Select Country',
  html_options: { onchange: 'this.form.requestSubmit()' }
) %>