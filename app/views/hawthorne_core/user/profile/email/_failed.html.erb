<%# v3.0 %>

<%
  message = 'Your new email doesn’t look valid<br>Please check for a typo and try again' if local_assigns[:syntax_error]
  message = 'Your new email is the same as your current one' if local_assigns[:identical]
  message = 'This email is already in use' if local_assigns[:taken]
%>

<%= render HawthorneCore::UI::Form::ErrorMessageComponent.new(message:) %>