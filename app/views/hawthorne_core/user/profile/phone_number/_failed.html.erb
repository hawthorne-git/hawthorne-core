<%# v3.0 %>

<%
  message = 'Your new phone number doesn’t look valid<br>Please check for a typo and try again' if local_assigns[:syntax_error]
  message = 'Your new phone number is the same as your current one' if local_assigns[:identical]
%>

<%= render HawthorneCore::UI::Form::ErrorMessageComponent.new(message:) %>